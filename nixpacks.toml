# Nixpacks configuration for Coolify deployment

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --production"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
